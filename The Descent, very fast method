import java.util.*;
import java.io.*;
import java.math.*;

class Player {

    public static void main(String args[]) {
        Scanner in = new Scanner(System.in);

        while (true) {
            ArrayList<Integer> tab = new ArrayList<Integer>();
            for (int i = 0; i < 8; i++) {

                int H = in.nextInt(); 
                tab.add(H);
            }
        System.out.println(tab.indexOf(Collections.max(tab)));
        }a
    }
}
